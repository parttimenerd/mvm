#ifndef CLASSES_HPP
#define CLASSES_HPP

class Env;

/**
 * @brief Adds basic classes to the passed environment
 * @param env passed environment
 */
void initClasses(Env *env);

#endif // CLASSES_HPP
